#include <stdio.h>

/**
 * main - Entry point of my programme
 *
 * Return: (0) on success
 */
int main(void)
{
	char str[] = "\"Programming is like building a multilingual puzzle";

	puts(str);

	return (0);
}
