#include <stdio.h>
#include <string.h>
#include "main.h"

/**
 * _puts - this print out a string
 *
 * @str: this the argument if the func
 *
 * Return: this func returns nothing
 */
void _puts(char *str)
{
        printf("%s\n", str);
}
