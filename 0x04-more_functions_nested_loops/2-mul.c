#include "main.h"

/**
* mul - this func this function multiply two numbers together
*
* @a: is the first argument of the func
* @b: is the second argument of the func
*
* Return: this returns 1 if is the char checked is true and 0 if otherwise
*
*/
int mul(int a, int b)
{
	int result;

	result = a * b;

	return (result);
}
